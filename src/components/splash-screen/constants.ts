export const LOADING_CODE_PT = `const carregarPortfolio = () => {
  return 'Pronto!';
};

carregarPortfolio();`;

export const LOADING_CODE_ES = `const cargarPortfolio = () => {
  return '¡Listo!';
};

cargarPortfolio();`;

export const LOADING_CODE_EN = `const loadPortfolio = () => {
  return 'Ready!';
};

loadPortfolio();`;
